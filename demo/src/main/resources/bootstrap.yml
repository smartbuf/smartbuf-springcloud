server:
  port: 8888
  http2:
    enabled: true

spring:
  application:
    name: demo
  profiles:
    active: native
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true
        service-name: demo
